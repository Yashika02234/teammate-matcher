# MongoDB Configuration
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.fvdd5yi.mongodb.net/?appName=Cluster0
DB_NAME=teammate_matcher

# JWT Authentication
# Use a strong secret key for production (e.g. openssl rand -hex 32)
JWT_SECRET=super-secret-default-key-for-dev

# CORS Configuration
# Comma-separated list of allowed origins. Use '*' for all origins in development.
ALLOWED_ORIGINS=*
